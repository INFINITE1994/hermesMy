[project]
name = "WebScraper"
version = "1.0.0"
description = "网页抓取工具 - 桌面应用"
requires-python = ">=3.10"
dependencies = [
    "PyQt6>=6.5",
    "requests>=2.31",
    "beautifulsoup4>=4.12",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends._legacy:_Backend"
