[project]
name = "clipboard-history"
version = "1.0.0"
description = "增强版剪贴板历史管理工具"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "PyQt6>=6.5",
    "Pillow>=10.0",
]

[project.scripts]
clipboard-history = "main:main"
