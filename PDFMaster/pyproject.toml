[project]
name = "pdfmaster"
version = "1.0.0"
description = "PDFMaster - 专业PDF处理工具，支持合并、拆分、压缩、水印、图片转换、文本提取"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "PyQt6>=6.5",
    "PyPDF2>=3.0",
    "Pillow>=10.0",
    "pdf2image>=1.16",
]

[project.scripts]
pdfmaster = "main:main"
