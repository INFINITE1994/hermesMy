[project]
name = "DiskBenchmark"
version = "1.0.0"
description = "磁盘性能基准测试工具"
requires-python = ">=3.10"
dependencies = [
    "PyQt6>=6.4.0",
    "psutil>=5.9.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
